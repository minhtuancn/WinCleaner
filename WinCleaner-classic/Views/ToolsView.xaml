<UserControl x:Class="WinCleaner.Views.ToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:WinCleaner.ViewModels"
             xmlns:v="clr-namespace:WinCleaner.Views"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="{DynamicResource BackgroundBrush}"
             Foreground="{DynamicResource TextPrimaryBrush}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/WinCleaner;component/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Tab Header Template with Icon -->
            <DataTemplate x:Key="TabHeaderTemplate">
                <StackPanel Orientation="Horizontal" Margin="8,4">
                    <Path Data="{Binding Icon}" Fill="{DynamicResource TextPrimaryBrush}" Width="16" Height="16" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding Title}" FontSize="{DynamicResource FontSizeNormal}" FontWeight="Medium" VerticalAlignment="Center"/>
                </StackPanel>
            </DataTemplate>

            <!-- Content Template Selector -->
            <DataTemplate DataType="{x:Type vm:SchedulerTabViewModel}">
                <v:SchedulerTab/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:CookieTabViewModel}">
                <v:CookieTab/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ShredTabViewModel}">
                <v:ShredTab/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:AppXTabViewModel}">
                <v:AppXTab/>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="{DynamicResource PageMargin}">
        <TabControl ItemsSource="{Binding Tabs}"
                    ItemTemplate="{StaticResource TabHeaderTemplate}"
                    ContentTemplateSelector="{x:Null}"
                    BorderThickness="0"
                    Background="Transparent"
                    Padding="0">
            <TabControl.Resources>
                <!-- Style for TabItem -->
                <Style TargetType="TabItem" BasedOn="{StaticResource {x:Type TabItem}}">
                    <Setter Property="Padding" Value="16,8"/>
                    <Setter Property="Margin" Value="0,0,4,0"/>
                </Style>
            </TabControl.Resources>
        </TabControl>
    </Grid>
</UserControl>